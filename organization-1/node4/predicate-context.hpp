#pragma once

#include "reference.hpp"

// IDEA:
// a predicate-context is a context defined by a set of predicates passing
// a predicate may also be provided to add things to the context
// if we added iterators or generators (with predicates) we could enumerate everything in the context
class predicate-context : public reference
{
};
