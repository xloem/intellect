# care about:
# - classes
# - annotation (@apple def bee, makes bee = apple(bee); @apple(three) makes bee = apple(three)(bee))
# - introspection
# can also use metaclasses to customize construction of classes
class node:
    pass
