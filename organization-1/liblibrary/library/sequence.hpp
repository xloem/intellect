#pragma onee
/*

namespace library {

class sequence
{
public:
	using element_type = T;

	virtual element_type* begin_pointer() = 0;
	virtual element_type* end_pointer() = 0;
	virtual element_type* increment_pointer(element_type* pointer) = 0;
};
*/

} // namespace library
